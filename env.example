# Supabase Configuration
# Obtenha essas variáveis no dashboard do seu projeto Supabase: https://app.supabase.com
# Copie este arquivo para .env.local e preencha com suas credenciais

# URL pública do projeto Supabase
NEXT_PUBLIC_SUPABASE_URL=your-project-url

# Chave pública anônima (safe to expose no client)
# Use UMA das duas opções abaixo (não ambas):
# Opção 1: Chave anon tradicional (JWT-based)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Opção 2: Chave publishable (nova - recomendada pelo Supabase)
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-publishable-key

# ID do projeto Supabase (usado para gerar tipos TypeScript)
# Encontre na URL do dashboard: https://app.supabase.com/project/<project-id>
SUPABASE_PROJECT_ID=your-project-id

# Chave de serviço (NUNCA exponha no client - apenas para Server Actions que precisam de permissões admin)
# Opcional: apenas se precisar de operações que bypassam RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# URL base do site (usado para links de confirmação de email)
# Em produção, use a URL do seu domínio (ex: https://seusite.com)
# Em desenvolvimento, pode deixar como http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000